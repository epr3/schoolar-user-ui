query Events($userId: ID!, $startDate: String, $endDate: String) {
  events: eventsByQuery(
    userId: $userId
    startDate: $startDate
    endDate: $endDate
  ) {
    id
    room
    date
    startTime
    endTime
    isFullDay
    isNotifiable
    subjectId
    groupId
    eventTypeId
    group {
      id
      number
    }
    subject {
      id
      name
    }
    eventType {
      id
      type
      color
    }
  }
}
